from setuptools import setup

setup(
    name='structio',
    version='1.0',
    author='James Wyatt',
    author_email='jamezo97@gmail.com',
    description='Simple wrapper around a bytestream to read values one at a time',
    keywords='struct io binary byte stream reader',
    packages=['structio'],
    data_files=[],
    install_requires=[],
    setup_requires=[],
    tests_require=[]
)
